export interface UserRecordModel {
    identityNumber: string;
    name: string;
    area: string;
    rol: string;
    state: string;
}

export interface ComputerAssignRecordModel {
    name: string;
    model: string;
    serialNumber: string;
    state: string;
}